# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

before_install:
  - tlmgr install index

r_build_args:
--no-build-vignettes

dist: trusty
os:
  - linux
  - osx
compiler:
  - clang
  - gcc
osx_image: xcode10
language: R

sudo: false
cache: packages



